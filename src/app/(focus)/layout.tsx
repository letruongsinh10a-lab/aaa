// Full-screen, distraction-free layout — no Header, no Sidebar.
// Used for: flashcard sessions, speaking practice, TOPIK test mode.
export default function FocusLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
